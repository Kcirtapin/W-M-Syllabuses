'use client'
import { useState } from 'react';
//

export default function Syllabus(props) {
    return (
    <div>
        <h1>Hello World</h1>
        <a href={props.name} target='_blank'>Click here for PDF</a>
    </div>);
}